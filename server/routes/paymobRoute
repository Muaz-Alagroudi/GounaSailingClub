const express = require("express");
const Router = express.Router();

Router.get("/", async (req, res) => {
  try {
    const msg = 'Hello from the paymob route!';
    res.status(200).json(msg);
  } catch (error) {
    console.log(error.message);
  }
});

module.exports = Router;
